---
title: 验证范围与检查记录
description: 实际构建、内容检查、本地运行和浏览器验收结果，以及未验证的外部实验。
---

# 验证范围与检查记录

检查日期：2026-09-14。来源提交：`25b7985f3b2dc37a84f4a7387ccd3c9f0e5b1595`。本页区分真实执行、静态阅读与外部实验；机器可读记录位于 `provenance/verification.json`。

## 网站与内容检查

| 检查 | 实际结果 |
| --- | --- |
| TypeScript / Vue 类型检查 | `npm run typecheck` 通过 |
| 静态构建 | `npm run build` 通过，生成 116 个 HTML 页面与 sitemap |
| 课程覆盖 | 19 / 19 章均有完整正文、教学补充、练习与来源 |
| 原材料导读 | 87 页；其中 38 份 Notebook 的英中单元格均对齐，执行代码取英文原文件 |
| 链接与资源 | VitePress 死链检查通过；另检查本地链接、所有构建 HTML 的内部目标、锚点和资源，0 错误 |
| 空正文检查 | 无“敬请期待”“待补充”“待实现”或占位视频入口 |
| 本地素材 | 98 张源图片、8 幅本项目中文 SVG；核心图片无需外链 |
| 教学辅助 | 47 个中英术语、路线、Python 小抄、FAQ、完整五阶段项目 |

验证使用 Node 18.20.5 / npm 10.8.2；推荐维护环境为 Node 22 LTS。Mermaid 的部分动态依赖块大于 500 kB，构建有体积警告，但未阻断生成。图表库按需加载，首次打开含 Mermaid 的页面仍有加载成本。

内容检查还比较原课程正文长度与代码块保留情况，防止导入规则误删原文。`provenance/notebook-alignment.json` 记录 38 份 Notebook 的单元格对应；`source-version.json` 记录 238 个源文件哈希。原课程中已指出的 SDK 历史差异仍应结合章节校订阅读。

## 浏览器实际操作

使用 Codex 内置浏览器，在 1440 × 1000 桌面视口和 390 × 844 手机视口进行验证；同时检查开发版与最终静态构建。

- 首页、三栏阅读、手机课程导航、长段落、表格与代码：没有整页横向溢出；宽表格/代码在各自区域滚动。
- 工具、RAG、多 Agent 三个演示：正常/失败分支、逐步推进、结束禁用、重置均可操作。
- 中文正文搜索与英文 `Idempotency` 搜索：命中对应章节/术语；术语表 `MCP` 筛选正常。
- 标记完成、侧栏勾选、刷新保存、继续学习、清除完成与上次位置：结果一致。
- 深浅主题切换及刷新保存、代码复制到剪贴板：实际操作通过。
- 图片弹窗放大、滚动和关闭；全部 15 个含 Mermaid 的原材料页与 3 个主章节图表：成功渲染。
- 直接访问/刷新 `.html` 页、404 返回首页、可见键盘焦点（3 px）：通过。
- 被检查页面未观察到控制台错误或警告。

这不是所有浏览器、所有屏幕与辅助技术的完整兼容性认证。没有声称测过真实手机设备或全套自动化无障碍审计。重新构建 `dist` 后需要重启静态 preview 服务，使它读取新资源清单。

## 已实际运行的 Python

| 实验 | 结果 | 范围 |
| --- | --- | --- |
| `first_agent.py` | 正常流程与一步预算停止 | 规则模拟、无 API |
| 综合项目行为测试 | 14 项通过 | 检索、授权、幂等、隔离、错误与日志 |
| 综合项目回归集 | 6 项通过 | 预设规则模型，不能代表真实大模型准确率 |
| 独立教学片段 | 5 个通过 | 规划就绪、酒店反思、协议推演、Python 基础与异步 |
| 原始签名收据 Notebook | 13 个非安装单元格通过 | 离线签名、验签、篡改、链与模拟工具 |
| 原始人工授权收据 Notebook | 6 个单元格通过 | 固定测试身份/时间，动作绑定与拒绝情形 |

Python 验证环境为 3.13.1。收据使用 `jcs==0.2.1`、`PyNaCl==1.6.0`、`cffi==2.1.1`、`pycparser==3.0`；摘要与详细原输出在 `provenance/receipt-verification.json`，独立片段记录在 `snippet-verification.json`。临时生成的密钥为教学值，没有使用用户身份或业务系统。

## 静态检查与未执行的实验

MAF 1.10.0 的 core/foundry/openai 发行源码已读取并核对关键调用签名；包哈希与 API 清单在 `provenance/sdk-reference.json`。新增 `cloud.py` 通过 Python 语法检查，未做云端请求。

以下均未执行：Foundry/Azure 模型、Azure AI Search、Mem0/Cognee 外部服务、真实 MCP/A2A 互操作、第三方网站 Browser-Use、Foundry Local 权重下载与推理、真实云部署和负载测试。它们需要独立账号、权限、平台环境、模型能力或可能产生费用的服务。

源 Notebook 下载保留原文件及历史输出。云端成功输出是上游保存的历史结果，不是本站实测；签名实验也不执行真实转账或 WebAuthn。正文未列入运行清单的其他片段属于导读或参考实现，不能从网站构建成功推断其外部服务均可运行。
